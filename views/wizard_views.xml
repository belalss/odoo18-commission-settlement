<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_x_commission_settlement_generate_wizard_form" model="ir.ui.view">
        <field name="name">x_commission_settlement_generate_wizard.form</field>
        <field name="model">x_commission_settlement_generate_wizard</field>
        <field name="arch" type="xml">
            <form string="Generate Settlements">
                <sheet>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <footer>
                        <button string="Generate" type="object" name="action_generate" class="btn-primary"/>
                        <button string="Cancel" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_x_commission_settlement_generate_wizard" model="ir.actions.act_window">
        <field name="name">Generate Settlements</field>
        <field name="res_model">x_commission_settlement_generate_wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>